{% extends 'base.html.twig' %}

{# @var location \App\Entity\Location #}
{# @var weather \App\Entity\Weather #}

{% block title %}Weather in {{ location.country }}, {{ location.city }}{% endblock %}

{% block body %}
    <main>
        <h1>Weather in {{ location.country }}, {{ location.city }}</h1>

        <ul>
            {% for measurement in measurements %}
                <li>{{ measurement.date|date('d.m.Y') }}: {{ measurement.celsius }}&deg;C</li>
            {% endfor %}
        </ul>
    </main>
{% endblock %}
